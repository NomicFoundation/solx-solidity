// RUN: solc --mlir-action=print-init --mmlir --mlir-print-debuginfo %s | FileCheck %s

pragma solidity ^0.8.0;

enum Direction { North, South, East, West }

struct Inner {
    uint256 x;
    bool[][] flags;
}

struct C {
    uint8   a;
    uint72[2] c72;
    bytes19 b;
    bytes17[3] c;
    string  d;
    uint256[] e;
    uint256 f;
    bool    flag;
    Direction direction;
    Inner   inner;
    address[2] fixedAddrArr;
    function(uint256) external fn_uint;
}

contract ClearTest {
    C[] arr;

    function pop() public { arr.pop(); }
}
// NOTE: Assertions have been autogenerated by test/updFileCheckTest.py
// CHECK: #Constructor = #sol<FunctionKind Constructor>
// CHECK-NEXT: #Contract = #sol<ContractKind Contract>
// CHECK-NEXT: #Default = #sol<RevertStrings Default>
// CHECK-NEXT: #NonPayable = #sol<StateMutability NonPayable>
// CHECK-NEXT: #Osaka = #sol<EvmVersion Osaka>
// CHECK-NEXT: module attributes {llvm.data_layout = "E-p:256:256-i256:256:256-S256-a:256:256", llvm.target_triple = "evm-unknown-unknown", sol.evm_version = #Osaka, sol.revert_strings = #Default} {
// CHECK-NEXT:   sol.contract @ClearTest_65 {
// CHECK-NEXT:     sol.state_var @arr_55 slot 0 offset 0 : !sol.array<? x !sol.struct<(ui8, !sol.array<2 x ui72, Storage>, !sol.fixedbytes<19>, !sol.array<3 x !sol.fixedbytes<17>, Storage>, !sol.string<Storage>, !sol.array<? x ui256, Storage>, ui256, i1, !sol.enum<3>, !sol.struct<(ui256, !sol.array<? x !sol.array<? x i1, Storage>, Storage>), Storage>, !sol.array<2 x !sol.address, Storage>, !sol.ext_func_ref<(ui256) -> ()>), Storage>, Storage> loc(#loc2)
// CHECK-NEXT:     sol.func @ClearTest_65() attributes {kind = #Constructor, orig_fn_type = () -> (), state_mutability = #NonPayable} {
// CHECK-NEXT:       sol.return loc(#loc1)
// CHECK-NEXT:     } loc(#loc1)
// CHECK-NEXT:     sol.func @pop_64() attributes {id = 64 : i64, orig_fn_type = () -> (), selector = -1527978708 : i32, state_mutability = #NonPayable} {
// CHECK-NEXT:       %0 = sol.addr_of @arr_55 : !sol.array<? x !sol.struct<(ui8, !sol.array<2 x ui72, Storage>, !sol.fixedbytes<19>, !sol.array<3 x !sol.fixedbytes<17>, Storage>, !sol.string<Storage>, !sol.array<? x ui256, Storage>, ui256, i1, !sol.enum<3>, !sol.struct<(ui256, !sol.array<? x !sol.array<? x i1, Storage>, Storage>), Storage>, !sol.array<2 x !sol.address, Storage>, !sol.ext_func_ref<(ui256) -> ()>), Storage>, Storage> loc(#loc2)
// CHECK-NEXT:       sol.pop %0 : !sol.array<? x !sol.struct<(ui8, !sol.array<2 x ui72, Storage>, !sol.fixedbytes<19>, !sol.array<3 x !sol.fixedbytes<17>, Storage>, !sol.string<Storage>, !sol.array<? x ui256, Storage>, ui256, i1, !sol.enum<3>, !sol.struct<(ui256, !sol.array<? x !sol.array<? x i1, Storage>, Storage>), Storage>, !sol.array<2 x !sol.address, Storage>, !sol.ext_func_ref<(ui256) -> ()>), Storage>, Storage> loc(#loc4)
// CHECK-NEXT:       sol.return loc(#loc3)
// CHECK-NEXT:     } loc(#loc3)
// CHECK-NEXT:   } {kind = #Contract} loc(#loc1)
// CHECK-NEXT: } loc(#loc)
// CHECK-NEXT: #loc = loc(unknown)
// CHECK-NEXT: #loc1 = loc({{.*}}:26:0)
// CHECK-NEXT: #loc2 = loc({{.*}}:27:4)
// CHECK-NEXT: #loc3 = loc({{.*}}:29:4)
// CHECK-NEXT: #loc4 = loc({{.*}}:29:28)
// CHECK-EMPTY:
